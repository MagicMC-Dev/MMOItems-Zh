package net.Indyuce.mmoitems.stat.data.type;

/**
 * <code>StatData</code> by itself is just a number, a piece of text, a boolean...
 * <p></p>
 * These are paired with an <code>ItemStat</code> to mean something, they then become the value of such <code>ItemStat</code>.
 */
public interface StatData { }
